# WalletConnect / Reown project ID (cloud.reown.com). Optional — without it the
# app still offers injected wallets (MiniPay, browser extensions) and MetaMask.
VITE_WALLETCONNECT_PROJECT_ID=

# Default vault address (can also be passed at runtime via ?vault=0x...)
VITE_VAULT_ADDRESS=
