# --- chain ---
RPC_URL=https://forno.celo.org
CHAIN_ID=42220

# --- deployed contracts ---
VAULT_ADDRESS=
# Protocol addresses default to verified Celo mainnet values (see config.js).

# --- keys (use separate keys per role; fund keeper with a little CELO for gas) ---
KEEPER_PRIVATE_KEY=
FEE_COLLECTOR_PRIVATE_KEY=

# --- decision parameters ---
MIN_DELTA_BPS=25
REBALANCE_PERIOD_DAYS=1
SAFETY_MARGIN=3
QUOTE_SLACK_BPS=10
INTERVAL_MS=21600000

# --- x402 fee settlement (public Celo facilitator, no API keys) ---
X402_FEE_ENDPOINT=http://localhost:4021/realize-fee
X402_FACILITATOR=https://api.x402.celo.org
TREASURY_ADDRESS=
FEE_SERVER_PORT=4021
MIN_FEE_TO_SETTLE=0.01
FEE_CLAIM_SYMBOL=USDm
